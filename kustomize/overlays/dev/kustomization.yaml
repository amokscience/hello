apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

bases:
  - ../../base

namePrefix: dev-

commonLabels:
  environment: development

images:
  - name: amokscience/hello
    newTag: dev-59f9ffa96ef3a0eb8c0590bf41e5a561c37606ef

configMapGenerator:
  - name: hello-config
    literals:
      - environment=dev
      - hello=Hello from dev Kubernetes!

patches:
  - target:
      kind: Deployment
      name: hello-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
