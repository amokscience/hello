apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

bases:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging

images:
  - name: amokscience/hello
    newTag: staging-e7f6a0c222b436407ae5c9761512e8a7bf1159cb

configMapGenerator:
  - name: hello-config
    literals:
      - environment=staging
      - hello=Hello from staging Kubernetes!

patches:
  - target:
      kind: Deployment
      name: hello-app
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
